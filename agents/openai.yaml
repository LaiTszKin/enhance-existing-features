interface:
  display_name: "enhance-existing-features"
  short_description: "Build brownfield features with dependency mapping and multi-module PRD gating"
  default_prompt: "Use $enhance-existing-features to extend an existing codebase, create a PRD for cross-module changes, and update tests."

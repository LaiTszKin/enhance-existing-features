interface:
  display_name: "enhance-existing-features"
  short_description: "Build brownfield features with dependency mapping"
  default_prompt: "Use $enhance-existing-features to extend an existing codebase with tests."
